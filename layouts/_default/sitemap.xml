{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml"
  xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {{- range .Data.Pages }}
  {{- if and .Permalink (ne .Kind "taxonomy") (ne .Kind "term") (ne .Params.robotsNoIndex true) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>
    {{- end }}
    <changefreq>{{ if .IsHome }}daily{{ else if eq .Section "posts" }}weekly{{ else }}monthly{{ end }}</changefreq>
    <priority>{{ if .IsHome }}1.0{{ else if eq .Kind "page" }}0.7{{ else if eq .Kind "section" }}0.5{{ else if or (eq .Kind "taxonomy") (eq .Kind "term") }}0.3{{ else }}0.5{{ end }}</priority>
    {{- if and (eq .Kind "page") (eq .Section "posts") }}
    {{- with .Params.image }}
    <image:image>
      <image:loc>{{ . | absURL }}</image:loc>
    </image:image>
    {{- end }}
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
