baseURL = 'https://tech.mdeeno.com/'
languageCode = 'ko-kr'
title = 'PropTech Lab'
theme = 'PaperMod'

[params]
  defaultTheme = "auto"
  ShowShareButtons = true
  ShowReadingTime = false
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  ShowCodeCopyButtons = true
  
  # 메인 화면 모드 (글 목록 스타일)
  homeInfoParams.Title = "🚀 PropTech Lab"
  homeInfoParams.Content = "부동산 데이터 분석 & 투자 전략 연구소"

  [params.cover]
    hidden = false
    hiddenInList = false
    hiddenInSingle = false
    responsiveImages = true

[outputs]
  home = ["HTML", "RSS", "JSON"] # 검색 기능을 위해 JSON 필수

# -----------------------------------------------------------
# 👇 [핵심] 상단 메뉴바 설정 (이모지로 가독성 UP)
# -----------------------------------------------------------
[[menu.main]]
  identifier = "archives"
  name = "🗂️ 전체보기"
  url = "/archives/"
  weight = 10

[[menu.main]]
  identifier = "categories"
  name = "📂 카테고리"
  url = "/categories/"
  weight = 20

[[menu.main]]
  identifier = "tags"
  name = "🏷️ 태그"
  url = "/tags/"
  weight = 30

[[menu.main]]
  identifier = "search"
  name = "🔍 검색"
  url = "/search/"
  weight = 40


[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true